# Allowance

Allowance is an open-source, local-first agent spending oversight tool that provisions virtual cards, enforces spending policies, and keeps a human in the loop for approvals.

## Quick Start

1. Download the appropriate binary for your platform.
2. Create a `.env` file based on `.env.example`.
3. Run `./allowance`.
4. Open `http://localhost:3000`.
